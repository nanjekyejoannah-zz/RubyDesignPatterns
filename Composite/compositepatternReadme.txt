The composite pattern is used to build the system frrom small parts and allowing us to treat the parts of the system without knowing whether they belong to a hierachy. In otherwards a parent object is accessed like a child object.

components.

base object also called a Component.
Another highlevel object also called a Composite.
leaf objects.


Algorithm.

Base object.
composite class implements the base object.

highlevel objects implementing the composite class.

leaf objects implementing the base class.

